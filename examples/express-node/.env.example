# neithly-monitor DSN for this example. Copy to `.env` and fill in.
# Format: https://<publicKey>@<host>/<projectId>
NEITHLY_DSN=

# Optional release tag attached to every captured event.
RELEASE=express-example-node@0.0.0

# Optional override; defaults to 3000.
PORT=3000
